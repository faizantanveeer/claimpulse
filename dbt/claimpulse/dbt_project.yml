
name: 'claimpulse'
version: '1.0.0'
profile: 'claimpulse'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


models:
  claimpulse:
    stagging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
